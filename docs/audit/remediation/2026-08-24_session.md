# Remediation Session — 2026-08-24

**Scope:** post-v1.0.0 independent audit remediation (criticals + hygiene + backlog).
**Commits:** `c915971` → `02756e8` → `736d42f` (on `main`, after `a40a577`).
**Method:** strict RED→GREEN TDD; every mutation gated through the
BoundaryValidator Zero-Trust gate (`validate_proposal` MCP tool) before edit.
**Evidence policy:** full unified diffs live in git (single source of truth).
This record maps findings to fixes, shows the critical hunks, and gives the
exact commands to regenerate every diff byte-for-byte.

---

## 1. Findings → Fixes

| Audit ID | Finding | Fix | Commit |
|---|---|---|---|
| C-NUEVO-1 🔴 | Security suite wrote `audit_log` rows into the real repo-root `traianus.db` (~4k rows): `validator.py` bound `DB_PATH` by value at import time; the autouse `isolate_db` monkeypatch could not rebind it. Also: the SEC-M-06 MCP subprocess test resolved the DB against CWD. | Lazy resolution via `storage.DB_PATH` at call time + tmp-CWD for the spawned server. Root-DB delta across a full suite run = **0**. | `02756e8` |
| C-NUEVO-2 🔴 | `POC_FREEZE_v1.md` promised determinism "identical across any execution environment", contradicting finding M1 (`docs/STATUS.md`). | Reworded to pinned-environment runtime determinism; bitwise cross-build guarantee explicitly disclaimed. | `02756e8` |
| A1 🟠 | `opencode.jsonc` instructed reading non-existent `docs/LOGOGRAPHY.md`. | Phantom instruction removed. | `02756e8` |
| P0b 🔴 (user-found) | Zero-Trust gate was fail-open on schema violations: `{}` → `EXECUTE_SAFE`; invented `Intent_Class:"HACK"` skipped mandatory literal grounding. | Strict `AgentMutationProposal.model_validate` after content screening; `Target_File` merged out-of-band; unknown enums / extra fields / non-dict → `INVALID_JSON`; present-but-invalid `Safety_Abort` → `BLOCKED_BY_SAFETY_GATE`. | `02756e8` |
| (latent) 🟠 | Contract divergence: normative `IntentClass` enum lacked `AUDIT`, yet validator logic, MCP tool docs and committed SEC-M-07 tests treat it as a grounding-gated mutating intent. | Enum gains `AUDIT`; schema caught up with its consumers. | `02756e8` |
| M3 🟡 | `/ingesta/vector` label flowed verbatim into persistent node ids and downstream edge ids (no charset/length bound). | Label contract `[A-Za-z0-9_-]{1,64}` else 422; empty label keeps digest node-id path by design. | `736d42f` |
| M4 🟡 | Six endpoints leaked exception internals via `detail=str(e)` in 5xx responses. | Fixed generic detail `Internal server error.`; internals stay server-side. 6 masking regressions added. | `736d42f` |
| A5 🟠 | `tools/mcp/spectral_math_mcp.py` re-implemented the C1 kernel (divergent-copy risk). | Delegates `critical_threshold` to `traianus.geometry.observables.calibrate_critical_threshold`; MCP layer keeps normalization + presentation stats only. | `736d42f` |
| (latent) 🔴→🟢 | Kernel hardening uncovered by dedup: canonical C1 returned **NaN for k=1 bases** (`np.var([])` appended unguarded); old MCP copy had the guard, the canonical kernel did not. | `if projections:` guard + empty-basis → `0.0` in the canonical kernel. | `736d42f` |
| M1 🟡 | Spanish residue in tracked docs despite seq 21 English policy (`Hallazgos…`, `Invariante…`). | AUDIT.md / ARCHITECTURE.md / PROJECT_IDENTITY.md translated. LEDGER history intentionally untouched (append-only doctrine). Zero residue outside the ledger. | `736d42f` |
| M2 🟡 | Logographic rule §6.4 violated: two primary MDs in `docs/architecture/contracts/`. | `POC_FREEZE_v1.md` isolated under `contracts/freeze/` (rename preserved 100%); README + INDEX links updated. | `736d42f` |
| hygiene ⚪ | Release freeze broken in working tree: uncommitted `[viz]` extra; stray empty `reddit.md`. | `[viz]` extra approved by operator and committed; `reddit.md` deleted manually by operator; root `traianus.db*` purged manually (re-created transiently by live-gate audit writes during the session — purge again after the last MCP call). | `c915971` / manual |

---

## 2. Commit Index

| Commit | Files | ± | Suite at commit |
|---|---|---|---|
| `c915971` build(deps): optional `[viz]` extra | 1 | +3 | 160 passed, 5 deselected |
| `02756e8` fix(security): lazy DB_PATH + strict gate conformance | 7 | +177 −12 | 160 passed, 5 deselected; root `audit_log` Δ=0 |
| `736d42f` fix(control-plane): backlog remediation | 13 | +274 −37 | **177 passed, 5 deselected** |

---

## 3. Critical Hunks (abridged)

### 3.1 Lazy DB binding — `traianus/security/validator.py`

```diff
-from traianus.storage import DB_PATH
+from traianus import storage
...
-        with sqlite3.connect(DB_PATH) as conn:
+        with sqlite3.connect(storage.DB_PATH) as conn:
```

### 3.2 Fail-closed schema conformance — same file (denylist now precedes schema)

```diff
-        safety_abort = proposal.get("Safety_Abort", "NONE")
-        block = proposal.get("Implementation_Block", "")
-        intent = proposal.get("Intent_Class", "NONE")
+        # Raw extraction first; content screening precedes protocol conformance.
+        safety_abort_raw = str(proposal.get("Safety_Abort", ""))
+        block = str(proposal.get("Implementation_Block", ""))
+        intent_raw = str(proposal.get("Intent_Class", ""))
...
         if any(token in block for token in forbidden):
             return _finalize({..., "ABORTED_VIOLATES_ZERO_TRUST",
+                              ...}, intent_raw, target_file_path, safety_abort_raw)
+
+        merged = dict(proposal)
+        merged.setdefault("Target_File", target_file_path)
+        try:
+            model = AgentMutationProposal.model_validate(merged)
+        except ValidationError as exc:
+            if any(err["loc"] == ("Safety_Abort",) and err["type"] != "missing"
+                   for err in exc.errors()):
+                return _finalize({..., "BLOCKED_BY_SAFETY_GATE", ...})
+            return _finalize({..., "INVALID_JSON", ...})
+
+        intent = model.Intent_Class.value
+        safety_abort = model.Safety_Abort.value
```

### 3.3 Label contract — `traianus/app.py`

```diff
+_SAFE_LABEL_RE = re.compile(r"^[A-Za-z0-9_-]{1,64}$")
...
     if body.label and not _SAFE_LABEL_RE.fullmatch(body.label):
+        log.warning("vector_ingestion_rejected", phase="validation", reason="unsafe_label")
+        raise HTTPException(status_code=422, ...)
     if body.label:
         node_id = f"VEC_{body.label}"
```

### 3.4 NaN kernel fix — `traianus/geometry/observables.py`

```diff
         if projections:
             base_variances.append(np.var(projections))
-    return float(np.mean(base_variances))
+    return float(np.mean(base_variances)) if base_variances else 0.0
```

(Without the `if projections:` guard, a k=1 basis appended `np.var([])` —
NaN — which then propagated into the consolidation threshold.)

---

## 4. Gate Case-ID Ledger

All proposals reached `EXECUTE_SAFE` except one deliberate quarantine that
exposed the self-referential denylist blind spot (see §6).

| Phase | Proposal | Decision | case_id |
|---|---|---|---|
| seq23 imports | FIX validator | EXECUTE_SAFE | `147a1bbd…` |
| seq23 dict-guard | FIX validator | EXECUTE_SAFE | `53a64724…` |
| seq23 connect | FIX validator | EXECUTE_SAFE | `e6ef9fc4…` |
| seq23 core v1 | FIX validator | **ABORTED_VIOLATES_ZERO_TRUST** | `25452ee9…` |
| seq23 extract | FIX validator | EXECUTE_SAFE | `a24296a1…` |
| seq23 schema | FIX validator | EXECUTE_SAFE | `e5558fb0…` |
| enum AUDIT | FIX proposals.py | EXECUTE_SAFE | `6e6c0566…` |
| missing-vs-flag | FIX validator | EXECUTE_SAFE | `d58a6890…` |
| SEC-M-06 CWD | FIX tests | EXECUTE_SAFE | `0efb5616…` |
| freeze wording | DOC POC_FREEZE | EXECUTE_SAFE | `cb36b9b8…` |
| phantom ref | FIX opencode.jsonc | EXECUTE_SAFE | `6ffddd4d…` |
| LEDGER seq 23 | DOC | EXECUTE_SAFE | `82b4290f…` |
| backlog app ×4 | FIX app.py | EXECUTE_SAFE | `fc9ba2b2 / 3d38d0b0 / 469c8dbd / 941919dd` |
| backlog mcp ×2+1 | REFACTOR mcp | EXECUTE_SAFE | `42189fe1 / 0d910c51 / b751ff3b` |
| stats len() | FIX mcp | EXECUTE_SAFE | `fe875198…` |
| kernel NaN ×2 | FIX observables | EXECUTE_SAFE | `a250db35 / fd9aba40` |
| label test fix | TEST | EXECUTE_SAFE | `7494da14…` |
| decontamination ×6 | DOC | EXECUTE_SAFE | `7d7c760b / 009edaa7 / 9a165d4c / d6bbd49c / 2287cb70 / 65c6d34e` |
| freeze move links ×2 | DOC | EXECUTE_SAFE | `cfc6bf4d / 6287aab2` |
| LEDGER seq 24 | DOC | EXECUTE_SAFE | `55a4f37c…` |
| this document | DOC | EXECUTE_SAFE | `6e54b727…` |

---

## 5. Verification Matrix

| Check | Result |
|---|---|
| Hermetic suite (final) | **177 passed, 5 deselected** (+34 vs session start: 143) |
| New regressions | isolation/schema 6 · masking 6 · label 9 · MCP guards 3 · (SEC-M-06 hardened) |
| Root DB leak | `audit_log` delta across full suite run = 0 (was >4000 accumulated) |
| Spanish residue outside LEDGER | 0 matches (`Hallazgos|Invariante`) |
| Logographic rule `contracts/` | compliant: 1 primary md + isolated `freeze/` subfolder |
| Forbidden network primitives scan | clean (unchanged) |

Reproduce any diff exactly:

```bash
git show c915971   # [viz] extra
git show 02756e8   # security hardening (seq 23)
git show 736d42f   # backlog remediation (seq 24)
```

---

## 6. Open Items

1. **Denylist self-reference (governance):** any proposal whose
   `Implementation_Block` contains the denylist's own literals is ungatable
   (case `25452ee9…`). Session worked around it by restructuring around the
   list without touching it. A durable decision for modifying the list
   (out-of-band operator path vs gate redesign) remains open — registered in
   LEDGER seq 23.
2. **RefApp-01 coexistence (RESOLVED — LEDGER seq 25):** PoC artifacts
   quarantined via `.gitignore` (`tools/results/`, `docs/manifesto/TOMO_0.md`,
   `Ethics_1.md`; plus global `node_modules/`, `.vite/`, `dist/`). The
   ε-bridge audit pair is now tracked (`tools/analyze_bridges.py` +
   `tests/unit/test_analyze_bridges.py`) making CI == local == 177.
   `frontend/` follows a SOURCES-ONLY policy (commit only package.json,
   lockfile, vite config, index.html, src/ once regenerated).
   Terminology canon: basin / relaminarize / VALIDATED-FALSIFIED
   (commit `0e9094a`).
3. **Manual purge:** because every live gate call writes one audit row,
   re-run after this session's last MCP call:
   `rm traianus.db traianus.db-shm traianus.db-wal`
