# 📜 Traianus — Repository Diff Log (2026-08-01)

> **Governance document (versionable).** Prepared by `@logographer` to record the
> **current repository diff** — the state of the working tree and the last commit against the
> baseline documented in `docs/STATE_CONSOLIDATION_2026-08-01.md` (which fixed HEAD at
> `76845a6`).
>
> **METHODOLOGICAL NOTE (important):** this `@logographer` session **did not have shell access**,
> so `git status`/`git diff`/`git log` could not be executed. The diff was **reconstructed**
> from readable sources in the working tree and `.git/`:
> `.git/HEAD`, `.git/refs/heads/ngi-candidacy`, `.git/logs/HEAD` (reflog), `.git/COMMIT_EDITMSG`,
> `.git/config`, `.git/FETCH_HEAD`, `.git/ORIG_HEAD`, `.gitignore` and full reading of the
> affected files. Every *committed vs. pending* assignment must be verified with
> `git status --short`, `git diff HEAD` and `git log --oneline -10` before any commit.
>
> **Scope:** ONLY documentation (`docs/`). This cycle does not modify source code, does not run
> tests, and does not create commits (AGENTS.md §4).

---

## 1. Git State

| Ref | Hash | Observation |
| --- | --- | --- |
| `ngi-candidacy` (HEAD) | `8d0ba22945ebbc353108b7ce58eb11834f47615f` (`8d0ba22`) | Current working branch (`.git/HEAD` → `refs/heads/ngi-candidacy`; verified in `.git/refs/heads/ngi-candidacy`) |
| `ngi-candidacy~1` (parent) | `76845a612c91d9c3a04880b8a0cce4fd90749c7b` (`76845a6`) | HEAD at closure of `STATE_CONSOLIDATION_2026-08-01.md` |
| `origin/main` | `e2ab8bc554fd6d31443b58d0e9a2c786e951d9c3` (`e2ab8bc`) | Diff base (unchanged) |
| `origin` | — | No new fetch: `FETCH_HEAD` stale (`d455f694`, historical fetch from `main`) |

**Branch not pushed to origin:** `ngi-candidacy` has no remote ref (`.git/config`: only
`vscode-merge-base = origin/main`); the full branch diff against `origin/main` remains
unpublished.

### 1.1 Reflog — the 6 commits on `ngi-candidacy` (since `e2ab8bc`)

Source: `.git/logs/HEAD` (lines 22–28).

| # | Short Hash | Date (epoch) | Message |
| --- | --- | --- | --- |
| 1 | `cd6aad58` | 1785606203 | `Audit cycle F1-F3: green C1 harness, append-only node log (H4), synced audit` |
| 2 | `b9a0f89b` | 1785611956 | `Harden opencode cycle #008: MCP validator v1.1.0, 5 subagents Zero-Trust, CI, quickstart` |
| 3 | `9983359` | 1785611958 | `Sync AGENTS.md constitution with 5-agent topology (plan-architect, orchestrator)` |
| 4 | `75b2809` | 1785612293 | `Materialize opencode commands/skills (F3-F4) and harden config (F5): permissions, providers, mcp_timeout` |
| 5 | `76845a6` | 1785615022 | `feat(audit): refactor traianus package, spec-first test suite and mcp validator v1.1.0aa` |
| 6 | **`8d0ba22`** | 1785618869 | **`feat(topologia): materializar adyacencia-epsilon E_n atomicamente en consolidacion (H5 / WP2)`** ← NEW since last consolidation |

**Key conclusion:** after the closure of `STATE_CONSOLIDATION_2026-08-01.md` (HEAD `76845a6`)
a **sixth commit** was made (`8d0ba22`, ~64 min later) that materializes ε-adjacency
`E_n` **atomically within the consolidation transaction** (audit finding H5 /
WP2 from roadmap). The consolidation document became **obsolete** in its git
state section (still citing `76845a6`).

---

## 2. Diff Summary

The current repository diff has **two layers**:

### Layer A — Commit `8d0ba22` (code + tests, already committed)

| What | Where | Why |
| --- | --- | --- |
| **Server-side** `EPSILON_EDGE` constant (`TRAIANUS_EPSILON_EDGE`, default `0.8`); never a client parameter (Zero-Trust) | `traianus/app.py:43-48` | H5 / ADR-023: deterministic server-side ε (RE-09/CO-12) |
| `persist_epsilon_edges(EPSILON_EDGE, conn=conn)` **within the same transaction** that inserts the consolidation revision | `traianus/app.py:454-459` | "Materialize E_n atomically in consolidation": on failure, rollback + HTTP 500 (H1/M5: no fake SUCCESS) |
| Shared helper `_current_node_vectors(conn)` (MAX(seq), excludes `telemetry_error`) | `traianus/app.py:576-588` | Refactored for E_n over current states |
| **CO-12** test (`test_consolidation_CO12_consolidate_persists_epsilon_edges`) + coverage header | `tests/bloques/consolidacion/test_especificos.py:245-328`, `:11` | Verifies consolidation persists `auto-edge-*` and preserves manual `edge-*` |
| **CO-12** MUST requirement in the consolidation SPEC | `docs/development/tests/SPEC-consolidacion.md:24` | Spec-First: the MUST preceded/coincided with the test |

> **Note:** `SPEC-consolidacion.md` lives under `docs/development/`, which is **gitignored**
> (`.gitignore:19`, Doc-Drift D2) → the CO-12 requirement is **not versioned** in any commit.

### Layer B — Observed working tree (changes WITHOUT commit in reflog)

| What | Where | Why |
| --- | --- | --- |
| Phases 1–3 consolidation document + §7 (debureaucratization) | `docs/STATE_CONSOLIDATION_2026-08-01.md` (new, untracked) | `@logographer`/`@plan-architect` governance cycle |
| D3/D4/D5 corrections + §5 entry in Logography | `docs/LOGOGRAPHY.md` | Doc sync (CL-I62 ACTIVE, `tests/afirmaciones/`, sections 1→6) |
| §9/§10 + **R2/D1 resolution note** | `docs/architecture/opencode_architecture.md:206-207,214` | Reference to consolidation; `TRAIANUS_AUDIT.md` exists after rename |
| Root instructions → `TRAIANUS_AUDIT.md` | `opencode.jsonc:6` | D1 resolved: `"instructions": ["TRAIANUS_AUDIT.md", "docs/LOGOGRAPHY.md"]` |
| **Rename** `TRAIANUS_AUDITORIA_ES.md` → `TRAIANUS_AUDIT.md` | root | D1/R2 (the referenced file now exists in the working tree) |
| **Deleted** `tools/contract_obligation_verifier.py` (205 lines) and `docs/templates/contract-mining/` (remaining empty directory) | `tools/`, `docs/templates/` | Record #013 — debureaucratization (governance decision) |
| Records #012 and #013 in the bitacora changelog | `docs/development/bitacora.md` (gitignored) | Cycle history (append-only) |
| Accidental heredoc artifact | `docs/development/SYNC_PLAN.md` (gitignored) | Contains the `cat << 'EOF'` that generated it; Phase 0 plan + raw sync |
| Canonical name of the claims SPEC | `docs/development/tests/SPEC-afirmaciones.md` (gitignored) | `tests/meta/_spec_lib.py:23` uses `SPEC-afirmaciones.md`; `LOGOGRAPHY.md:25` still says "claims" |

> **Ignored artifacts (not part of the diff):** `traianus.db`, `traianus.db-wal`,
> `traianus.db-shm`, `.venv/`, `traianus.egg-info/`, `repomix-output.xml`, `__pycache__/`,
> `.DS_Store` (see `.gitignore`).

---

## 3. Affected Files Table

| Path | Observed Status | Layer | Description |
| --- | --- | --- | --- |
| `traianus/app.py` | Modified | **Commit `8d0ba22`** | E_n: server-side `EPSILON_EDGE` + atomic persistence in `/consolidar` + `_current_node_vectors` |
| `tests/bloques/consolidacion/test_especificos.py` | Modified | **Commit `8d0ba22`** | CO-12 test (E_n in consolidation); coverage expanded to `..., CO12` |
| `docs/development/tests/SPEC-consolidacion.md` | Modified | Worktree (gitignored) | **CO-12** MUST requirement (H5/ADR-023) |
| `docs/STATE_CONSOLIDATION_2026-08-01.md` | Added (untracked) | Worktree | Phases 1–3 consolidation + §7; git state **obsolete** (76845a6) |
| `docs/LOGOGRAPHY.md` | Modified | Worktree | D3/D4/D5 + consolidation entry (§5) |
| `docs/architecture/opencode_architecture.md` | Modified | Worktree | §9 (consolidation ref + R2 note), §10 (anchors 174 passed) |
| `opencode.jsonc` | Modified | Worktree | Instructions → `TRAIANUS_AUDIT.md` |
| `TRAIANUS_AUDITORIA_ES.md` → `TRAIANUS_AUDIT.md` | Renamed | Worktree | D1/R2: root referenced file restoration |
| `tools/contract_obligation_verifier.py` | Deleted | Worktree | Record #013 (dead material) |
| `docs/templates/contract-mining/` | Deleted (empty dir remains) | Worktree | Record #013 |
| `docs/development/bitacora.md` | Modified | Worktree (gitignored) | Records #012, #013 |
| `docs/development/tests/TEST_OVERVIEW.md` | Modified | Worktree (gitignored) | Measured state; **D7 persistent** (cites `9983359`) |
| `docs/development/SYNC_PLAN.md` | Added | Worktree (gitignored) | Accidental heredoc artifact |
| `docs/development/tests/SPEC-afirmaciones.md` | Present | Worktree (gitignored) | Canonical name vs "claims" in LOGOGRAPHY (D11) |
| `.github/workflows/ci.yml`, `.opencode/agents/*`, `.opencode/command/*` | Unchanged in this cycle | — | Already existed in `76845a6` (verified in working tree) |

---

## 4. Diff Blocks by File

### 4.1 `traianus/app.py` — atomic materialization of E_n (H5 / WP2)

**New — server-side constant (Zero-Trust, never a client parameter):**

```python
# =====================================================================
# SERVER-SIDE EPSILON FOR DETERMINISTIC E_n (ADR-023/H5, RE-09/CO-12)
# Epsilon is a server-side constant, never a client parameter (Zero-Trust).
# Configurable only at boot via TRAIANUS_EPSILON_EDGE.
# =====================================================================
EPSILON_EDGE = float(os.environ.get("TRAIANUS_EPSILON_EDGE", "0.8"))
```

**New — atomic call within the consolidation transaction** (`consolidate_sovereignty`):

```python
            # H5/ADR-023 + RE-09/CO-12: persist deterministic E_n
            # (auto-edge-*) over current MAX(seq) vectors, in the SAME
            # transaction as the revision. Unconditional (both consolidated
            # and incubating branches). Any sqlite3.Error propagates to the
            # outer except -> HTTP 500 + rollback (H1/M5: no fake SUCCESS).
            persist_epsilon_edges(EPSILON_EDGE, conn=conn)
```

**New/refactored — shared read helper and transactional persistence signature:**

```python
def _current_node_vectors(conn: sqlite3.Connection) -> dict[str, np.ndarray]: ...
def persist_epsilon_edges(epsilon: float, conn: sqlite3.Connection | None = None) -> int:
    # Replaces ONLY the previous auto-edge-* subset (DELETE by id prefix);
    # manual edge-* rows are preserved (H4). If `conn` is provided, operates
    # inside that transaction (no commit — the caller owns the transaction)...
```

**Pre-existing context (already in `76845a6`, NOT part of this diff):**
`_compute_epsilon_edges` / `rebuild_epsilon_edges` (RE-08) and `persist_epsilon_edges` without
`conn` parameter (RE-09), documented in `STATE_CONSOLIDATION` §6.1. Commit `8d0ba22`
makes persistence **atomic within `/consolidar`** and fixes ε **on the server**.

### 4.2 `tests/bloques/consolidacion/test_especificos.py` — CO-12

New hermetic test (fake encoder, `monkeypatch` threshold to `0.0` to force topological
key): seeds `NODE_A`/`NODE_B` at distance `≤ EPSILON_EDGE`, creates a manual edge
`edge-NODE_A-NODE_B` (state=`manual`) and consolidates `NODE_A`; asserts:

```python
    assert auto_row is not None, (
        "CO-12 MUST: consolidation must persist auto-edge-NODE_A-NODE_B (E_n)"
    )
    assert auto_row[3] == "auto"
    assert manual_row is not None, "CO-12 MUST: manual edge-* must survive E_n"
    assert manual_row[3] == "manual"
```

Expanded coverage header (`test_especificos.py:11`):
`Coverage: CO01, CO02, CO03, CO04, CO05, CO06, CO07, CO11, CO12`.

### 4.3 `docs/development/tests/SPEC-consolidacion.md` — CO-12 (gitignored)

```markdown
- **CO-12** MUST: Consolidar un nodo reconstruye y persiste E_n (aristas auto-edge-* con state='auto') sobre las revisiones actuales MAX(seq), sin alterar las aristas manuales edge-* (H5/ADR-023).
```

### 4.4 Documentary layer (worktree) — key fragments

- `opencode.jsonc:6` — `"instructions": ["TRAIANUS_AUDIT.md", "docs/LOGOGRAPHY.md"]`
  (was: `["TRAIANUS_AUDITORIA_ES.md", "docs/LOGOGRAPHY.md"]`).
- `docs/architecture/opencode_architecture.md:207` — R2 note:
  `**Note (R2):** TRAIANUS_AUDIT.md is referenced in opencode.jsonc:6 ... and it exists in the working tree after the 2026-08-01 rename from TRAIANUS_AUDITORIA_ES.md (Doc-Drift D1 resolution).`
- `docs/architecture/opencode_architecture.md:214` — state anchor:
  `**Pytest (current state, 2026-08-01):** python3 -m pytest tests/ -q → 174 passed / 2 skipped`.
- `docs/LOGOGRAPHY.md:28` — `STATE_CONSOLIDATION_2026-08-01.md` entry in §5 (Audit).
- `docs/development/bitacora.md:160-181` — Records #012 (logographic sync) and #013
  (debureaucratization: removes `tools/contract_obligation_verifier.py` and
  `docs/templates/contract-mining/`; regression `pytest tests/ -m "not model" -q` → 169 passed,
  2 skipped, 7 deselected).

---

## 5. Relationship to `AGENTS.md` Invariants and Audit Status

| # | Invariant | Status | Evidence (path:line) |
| --- | --- | --- | --- |
| 1 | Consult `TRAIANUS_AUDIT.md` before refactoring `traianus/app.py` | ⚠️ **D10 worsened (R3 NOT applied)** | Commit `8d0ba22` implements `E_n` in `traianus/app.py:43-48,454-459`, but `TRAIANUS_AUDIT.md:78` still declares: `` `E_n` (ε-adjacency) and `K_n` (faces) still unimplemented; `manifold_edges` only written via manual `POST /relations`. `` — audit-vs-code contradiction |
| 2 | Exclude self-projection ($i = j$) when calibrating `auto_calibrate_critical_threshold()` | ✅ Verified | `traianus/app.py:262` — `for j, other in enumerate(vectors) if j != i`; regression `test_consolidation_CO02_threshold_excludes_self_projection` (no changes in this cycle) |
| 3 | Zero-Trust and network: no external requests; Uvicorn on `127.0.0.1`; no wildcard CORS with credentials | ✅ Verified | `EPSILON_EDGE` is **server-side** (`app.py:43-48`), never a client parameter; enumerated CORS (`ALLOWED_ORIGINS`, `app.py:30`); token on mutating routes (`require_token`); local MCP stdio without network |
| 4 | Literal Grounding: the citation in `Topological_Grounding` must exist exactly in the source | ✅ Verified (E_n citations) | ADR-023/H5, RE-08/RE-09 and CO-12 comments in `app.py:454-459,591-659` and `test_especificos.py:245-328`; ADR-025 citation in `app.py:122` (pre-existing) |
| 5 | Immutable Persistence (*Append-Only*): no `UPDATE`/`DELETE` on node history; revisions with increasing `seq` | ✅ Nodes / ⚠️ nuance on edges | `manifold_nodes` still PK `(id, seq)` without UPDATE/REPLACE/DELETE (CO-06/CO-07); `persist_epsilon_edges` **DELETEs only the `auto-edge-%` subset** from `manifold_edges` (edges, not node history) and preserves manual `edge-*` — intentional and documented in `app.py:625-633` |

### Audit Status (TRAIANUS_AUDIT.md vs reality post-`8d0ba22`)

| Finding | Documented Status (audit) | Real Status (working tree) |
| --- | --- | --- |
| C1 (threshold) | ✅ Resolved | ✅ Resolved — no changes |
| H1–H3, M3–M7 | ✅ Resolved | ✅ Resolved — no changes |
| **H4** | 🟠 Open (partial) — "`manifold_edges` still mutable" | 🟠 Partial — nodes append-only; auto edges replaceable by design |
| **H5** | 🟠 Open — "`E_n` … still unimplemented" | 🟠 **Advanced** — `E_n` computed (RE-08) and **atomically materialized in consolidation** (CO-12/`8d0ba22`); `K_n` still pending (WP2) |
| M8 | 🟡 Open | 🟡 Open — `flake.lock` uncommitted (R5) |

---

## 6. Notes for `@logographer` (Future Updates — Doc-Drift Prevention)

1. **Obsolete git state of consolidation:** `docs/STATE_CONSOLIDATION_2026-08-01.md` §1 fixes
   HEAD at `76845a6`; current HEAD is `8d0ba22`. The next Logography cycle must record
   a **Record #014** in `bitacora.md` (append-only) and refresh/create the consolidation with
   the new state; this `DIF_LOG` is the seed for that cycle.
2. **Persistent D7:** `docs/development/tests/TEST_OVERVIEW.md:3,242` cites commit `9983359`;
   must be refreshed to `76845a6` (and evaluate whether to anchor `8d0ba22` + CO-12 test and its count).
3. **D11 (new):** `docs/LOGOGRAPHY.md:25` lists `SPEC-{…, claims, …}.md`; the actual file is
   `SPEC-afirmaciones.md` (canonical in `tests/meta/_spec_lib.py:23`). Fix the line.
4. **D12 (new, measurement):** `bitacora.md` #013 reports `169 passed` for `-m "not model"`,
   while `TEST_OVERVIEW.md` §2/§7 reports `167 passed`. Verify with `pytest` and unify.
5. **D2 persists (critical for CI):** `.gitignore:19` still ignores `docs/development/` →
   SPECs, bitacora, TEST_OVERVIEW and SYNC_PLAN are **not versioned**; meta-guardians
   (`tests/meta/_spec_lib.py:13`) read `docs/development/tests/` at runtime → hermetic CI
   breaks on fresh clone (consolidation recommendation **R1**, still unapplied).
6. **Doc-vs-doc R2/D1:** `opencode_architecture.md:207` declares R2 resolved (rename to
   `TRAIANUS_AUDIT.md`), but `STATE_CONSOLIDATION` §5 still lists D1/R2 as pending —
   update consolidation in the next cycle (working tree evidence supports the
   resolution: the file exists and `opencode.jsonc:6` references it).
7. **Logography:** add the entry for this document (`docs/DIF_LOG_2026-08-01.md`) to
   section 5 of `docs/LOGOGRAPHY.md` when `@orchestrator` approves.
8. **Optional cleanup:** `docs/development/SYNC_PLAN.md` is an accidental heredoc artifact
   (gitignored); decide whether to rewrite as a real document or delete it.
9. **Pending verification with git (next session with shell):** confirm which Layer B files are
   committed in `8d0ba22` vs. pending in the working tree; run
   `git status --short`, `git diff HEAD --stat` and `git log --oneline -10`; and re-measure the suite
   after CO-12 (`pytest tests/ -q`).

---

## 7. Literal Citations Verified in This Session (CL-LIT1)

Each citation was checked character-by-character against the working tree:

| Path:Line | Verified Citation |
| --- | --- |
| `traianus/app.py:48` | `EPSILON_EDGE = float(os.environ.get("TRAIANUS_EPSILON_EDGE", "0.8"))` |
| `traianus/app.py:459` | `persist_epsilon_edges(EPSILON_EDGE, conn=conn)` |
| `traianus/app.py:262` | `for j, other in enumerate(vectors) if j != i` (C1, no changes) |
| `tests/bloques/consolidacion/test_especificos.py:322` | `"CO-12 MUST: consolidation must persist auto-edge-NODE_A-NODE_B (E_n)"` |
| `tests/bloques/consolidacion/test_especificos.py:11` | `Coverage: CO01, CO02, CO03, CO04, CO05, CO06, CO07, CO11, CO12` |
| `docs/development/tests/SPEC-consolidacion.md:24` | `- **CO-12** MUST: Consolidar un nodo reconstruye y persiste E_n … (H5/ADR-023).` |
| `docs/development/tests/SPEC-consolidacion.md` (name) | actual file `SPEC-afirmaciones.md` vs `LOGOGRAPHY.md:25` "claims" (D11) |
| `TRAIANUS_AUDIT.md:78` | `` `E_n` (ε-adjacency) and `K_n` (faces) still unimplemented `` (D10) |
| `.gitignore:19` | `docs/development/` (D2) |
| `opencode.jsonc:6` | `"instructions": ["TRAIANUS_AUDIT.md", "docs/LOGOGRAPHY.md"]` |
| `docs/architecture/opencode_architecture.md:207` | `**Note (R2):** TRAIANUS_AUDIT.md … exists in the working tree after the 2026-08-01 rename` |
| `.git/logs/HEAD:28` | `76845a612c91d9c3a04880b8a0cce4fd90749c7b 8d0ba22945ebbc353108b7ce58eb11834f47615f … commit: feat(topologia): materializar adyacencia-epsilon E_n atomicamente en consolidacion (H5 / WP2)` |