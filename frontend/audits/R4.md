# R4 — neighbourhood kept in a 5D perspective

**Question.** In a perspective chosen at note q, the vertical axis already orders every note by its
real distance to q. Do the four other observed dimensions — the horizontal axis λ and the three
colour channels — keep q's real neighbours together, or scatter them more than an arbitrary
horizontal axis would?

**Refuter (R4).** The operator's 5D perspective scatters q's real neighbours more than the same
perspective with an arbitrary horizontal axis, by the rule below.

## Instrument audit record (revision 4)

```
Instrument audit — R4, neighbourhood recall in a 5D perspective (revision 4)

Script: tools/experiments/r4_perspective_recall.py; unit tests:
  tests/unit/test_r4_perspective_recall.py. Perspective function: traianus/geometry/perspective.py
  (built on day 3 with TDD; the /spatial?anchor= endpoint and this script call the same function,
  which returns the z-scored coordinates below; the client renders what it returns).
K6 dependency: R4 uses the channels selected by the K6 run whose result is committed and whose
  commit hash is added to this record before R4's first run — the only edit allowed after review
  without a new review. If that K6 run is invalid (any K6 validity condition fails, e.g. dipole 1
  on the fallback), R4 does not run. If K6 selects m < 3 channels, the perspective is
  (2 + m)-dimensional; m = 0 → R4 runs in 2D and says so.
Data: definitions.md, restricted to EVAL of frontend/audits/K6.md (odd indices, N = 1,110). The
  script refuses to run unless the two sha256 digests fixed in K6's Data line match, which fixes
  the row order and so makes this EVAL the set K6 selected its channels on. K6's axis ranking
  was fitted on FIT; its channel selection ran on EVAL — not leakage of neighbour structure
  (selection reads no neighbours), but declared. Every q in EVAL is a chosen note, in index
  order. Vectors re-normalised to unit length in float64 after the float32 cast; one Gram array
  G = V Vᵀ (float64) supplies both the 384-d cosine ground truth and every y = ⟨v, q̂⟩.
Overrides definitions.md "Position": here c₁ = q.
Perspective at q (operator arm): c₁ = q; poles = the two axes â_k with the largest ⟨q, â_k⟩,
  ties by axis id ascending, c_A the larger. Frame from PolarProjector.prepare
  (polar_projector.py:190-247), once per q. x = ⟨v, v_dipole⟩ / ‖v_dipole‖², unclipped (the
  perspective view renders unclipped); y = ⟨v, q̂⟩. Collinear fallback (:184-188) counted per q
  and reported; the arm keeps the fallback dipole, as the operator does.
Colour: the channels K6 selects, each with the formula K6 uses, unclipped, in K6's per-epoch
  frame (P⊥ relative to the epoch anchor ĉ₁, not to q) — fixed per note, independent of q —
  recomputed here on the re-normalised float64 vectors (they differ from K6's by ≈ 1e-7):
    λ₂ = ⟨v, v_dipole,2⟩ / ‖v_dipole,2‖²;  λ₃ = ⟨v, v_dipole,3⟩ / ‖v_dipole,3‖²;
    a₈ = ⟨v, P⊥â_8⟩ / ‖P⊥â_8‖²;  l = 1/(1 + Var) of the 8 projections ⟨v, â_k⟩ (ddof = 0).
  R4 measures the coordinates the client receives; how they map to perceived colour (LCh) is a
  rendering step R4 does not measure — declared limit.
Observed space: the 2 + m coordinates, each z-scored over the N − 1 notes other than q
  (population s.d.), and q mapped by the same transform. Equal weights, by the author's design
  principle that the dimensions are independent. K6 tested that principle only against K6's
  epoch-anchored position; R4's position is anchored at q, so for each colour channel R4 reports,
  descriptive only, its R² on (1, x, y, x², y², xy) of this perspective, averaged over q.
Column "recall@15": for q, of q's 15 nearest notes in 384-d (largest G[q, ·]; drawn from the same
  N − 1 = 1,109 EVAL notes; q excluded; ties by lower index), how many are among q's 15 nearest
  in the observed space (Euclidean; q excluded; ties by lower index). k = 15 is a design choice —
  the notes one inspects at a glance around a chosen note; k = 5 and 50 reported, not deciding.
Arms (operator, random and y-only on the same q, notes, y, colours, z-scoring, distance and tie
  code; the permutation control shares only the recall function):
  Operator — (x, y, colours) as above.
  Random horizontal axis (deciding) — (⟨v, u_j⟩, y, colours) with u_j = P_q⊥e_j / ‖P_q⊥e_j‖,
    P_q⊥x = x − ⟨x, q̂⟩q̂: the same y as the operator and a horizontal direction orthogonal to q,
    as v_dipole is. Bank of J = 200 directions e_j uniform on S^383. Why uniform: v_dipole is
    fixed by the basis, not by the data, so the reference is a direction also chosen without the
    data (the argument K6 uses for its deciding null).
  Random horizontal axis, Σ-weighted (reference only) — as above with e_j replaced by
    g_j = X_cᵀ f_j / √1110 (X_c the centred EVAL matrix), 200 directions; D̄ reported, never used
    by the rule.
  y-only control — ranking by y alone through the same distance and tie code (1-D Euclidean).
    Must give 15/15 for every q. A mismatch at the 15th place counts as a tie, not a failure, iff
    the 15th and 16th similarities to q differ by < 1e-12; ties and any note with
    G[q, ·] ≥ 1 − 1e-12 (near-duplicates) are counted and reported. Any other mismatch fails.
  Permutation control — per q, the neighbour list is the first 15 entries of
    rng.permutation(1109), indexed into the N − 1 notes other than q in index order, scored by
    the same recall function. Expected recall 225/1109 = 0.2029, per-q s.d. ≈ 0.445
    (hypergeometric), s.d. of the mean over 1,110 q ≈ 0.0133. Must fall within
    0.2029 ± 4 × 0.0133, widened to [0.149, 0.257].
Dependence between notes: EVAL alternates chunks of a sequential text, so nearby q share
  neighbourhoods and their D_q are correlated. Block length L, fixed from the ground truth alone,
  before any arm is computed: overlap(g) = mean over i of |N15(i) ∩ N15(i + g)| / 15 in EVAL index
  order; far = the same mean over all pairs with |i − j| ≥ 100. L = the smallest g ≥ 1 with
  overlap(g) ≤ 2 × far, capped at 50 (if the cap binds, L = 50 and it is declared). EVAL is cut
  into ⌈1110 / L⌉ contiguous blocks in index order (the last may be shorter).
Draws: rng = numpy.random.default_rng(20260918) (PCG64), used in this order and nowhere else:
  E = rng.standard_normal((200, 384)) (row j is e_j); then, for q in EVAL index order,
  rng.permutation(1109); then, for b = 1…10,000, K_b = rng.integers(0, n_blocks, n_blocks)
  followed by J_b = rng.integers(0, 200, 200); then F = rng.standard_normal((200, 1110)) for the
  Σ-weighted reference.
Statistic: D_q = recall_operator(q) − mean over j of recall_random(q, u_j). For resample b, Q_b =
  the notes of the blocks in K_b, and D̄_b = mean over q in Q_b of
  [recall_operator(q) − mean over j in J_b of recall_random(q, u_j)].
  D̄ = the same with all q and all 200 directions.
Rule: 95% interval = [sorted(D̄_b)[249], sorted(D̄_b)[9749]]. R4 holds (the operator scatters
  more than an arbitrary axis) iff the upper bound < 0; otherwise R4 does not hold. Resampling
  blocks of notes and directions makes the conclusion about an arbitrary horizontal direction
  orthogonal to q; resampling directions inside every note's term overstates the random arm's
  spread, so the interval is conservative — declared. B = 10,000: the rank of a 2.5% or 97.5%
  quantile has Monte Carlo s.d. √(B·0.025·0.975) ≈ 16 ranks; the neighbours sorted[233] /
  sorted[265] and sorted[9733] / sorted[9765] are reported to show that a decision does not hinge
  on Monte Carlo noise.
Reported, not deciding: retention = mean recall_operator / 15; the same statistic in 2D (x, y
  only) and the difference 5D − 2D per arm (what colour recovers); k = 5 and 50; fallback count;
  the interval with block length 2L; the Σ-weighted reference; the colour channels' R² on this
  perspective's position.

Same thing in every arm?   Operator, random and y-only arms: same q, notes, y, colour channels,
                           z-scoring, neighbour, distance and tie code; operator and random differ
                           only in the horizontal direction (v_dipole against u_j, both ⊥ q). The
                           permutation control shares only the recall function.
Leakage?                   By construction y = ⟨v, q̂⟩ is monotone in the 384-d distance: every arm
                           carries the ground-truth order in one dimension. The column therefore
                           measures how much the other dimensions spoil that order, not whether
                           neighbours are found from scratch; the y-only control is the ceiling.
                           The block length reads the ground truth only, never an arm.
Comparable arms?           Valid only if the y-only control passes (ties as defined above) and the
                           permutation control lands in [0.149, 0.257]. If either fails, the run is
                           invalid and no result is used. The overview is not an arm: it has no q.
Text matches code?         Checked at review (phase 2).
Reviewed by:               pending (revision 4).
```

## Review history

- **Revision 1** — `instrument-audit` (Claude Opus 5), 2026-09-18, phase 1, commit `4a52417`:
  CHANGES (8 blocking, 3 non-blocking). Blocking: no decision rule or aggregation; no control
  that can fail; leakage answered wrongly — y = ⟨v, q̂⟩ and ‖v − q‖ are the ground-truth order;
  q selection and aggregation unstated; q not excluded in 2D, metric and ties unnamed; radial
  arm had no code path; overview arm unspecified (post hoc risk); seed unnamed and clipping ties.
  Non-blocking: R4 overrides the shared position; rendered scaling unstated; pole ties and
  fallback count. Addressed in revision 2, which also reformulates the question (scope change
  logged in POC.md): the perspective is measured in 5D, as the author specified, with equal
  weights because the dimensions are independent by design.
- **Revision 2** — Claude Opus 5, `instrument-audit`, blind session, 2026-09-18, phase 1, commit
  `b533c67`: CHANGES (3 blocking, 5 non-blocking). Blocking: the radial arm differed in more than
  the horizontal axis (√(2 − 2⟨v, q̂⟩) vertical, e_j with a component along q); the interval
  resampled q but held the 20 directions fixed, and 20 had no derivation; colour channels'
  formulas and clipping unstated. Non-blocking: y-only control's shared arrays and code; neighbour
  pool; "never saw" overstated for K6's selection; exact draw calls; K6 run not pinned and its
  invalid case not handled. Addressed in revision 3: random arm shares y and uses u_j = P_q⊥e_j;
  two-level bootstrap over q and a bank of 200 directions; exact unclipped channel formulas in the
  epoch frame with the perceived-colour limit declared; one Gram array for ground truth and y;
  pool of 1,109; K6 selection on EVAL declared; draw order fixed; K6 pinned by commit before R4
  runs, R4 skipped if K6 is invalid.
- **Revision 3** — Claude (Opus 5), `instrument-audit`, blind session, 2026-09-18, phase 1, commit
  `d1afa0d`: CHANGES (1 blocking, 7 non-blocking). Blocking: the bootstrap resampled single notes
  as if independent, while EVAL alternates chunks of a sequential text, so the interval was too
  narrow. Non-blocking: the permutation control's neighbour list and its exception from the
  shared distance code; sha256 check for EVAL's row order; "independent … tested by K6" overstated
  for a q-anchored position, and who applies the z-scoring; uniform vs. Σ-weighted random
  directions; y-only control's float ties; the K6 hash as the one post-review edit; B = 10,000,
  the conservative two-level resampling, and colour recomputation on re-normalised vectors.
  Addressed in revision 4: block bootstrap with L fixed from ground-truth neighbour overlap only
  (2L reported); all seven non-blocking items. The reviewer's keyword search surfaced single
  lines of K6's review history, against definitions.md; none held a result or R4's hypothesis.
